/**
 * @param {number} rows
 * @param {number} cols
 * @param {number} rCenter
 * @param {number} cCenter
 * @return {number[][]}
 */
var allCellsDistOrder = function(rows, cols, rCenter, cCenter) {
    let out = [];
    let q = [[rCenter, cCenter]];
    let vis = Array({length:rows,})
    while()
    return out;
};