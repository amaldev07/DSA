function countGoodSubstrings(s: string): number {
    let sArr: String[] = s.split('');
    let count = 0;
    let subArr: String[] = [];
    for (let i = 0; i < 3; i++) {
        subArr.push(sArr[i]);
    }
    // if()
    for (let i = 3; i < sArr.length; i++) {
        
    }
    return count;
};